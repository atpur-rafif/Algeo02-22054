{
  "dependencies": {
    "express": "^4.18.2"
  },
  "scripts": {
    "build-app": "make build",
    "build-web": "npx swc web -d dist -D",
    "build": "npm run build-app; npm run build-web",
    "all": "npm run build; node ./dist/main.js"
  },
  "devDependencies": {
    "@swc/cli": "^0.1.62",
    "@swc/core": "^1.3.95",
    "@types/express": "^4.17.20"
  }
}
